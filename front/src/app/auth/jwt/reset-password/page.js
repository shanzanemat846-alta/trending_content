// sections
import { JwtResetPassword } from 'src/sections/auth/jwt';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Jwt: Reset Password',
};

export default function ResetPasswordPage() {
  return <JwtResetPassword />;
}
